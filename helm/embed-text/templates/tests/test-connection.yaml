apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "embed-text.fullname" . }}-test-connection"
  labels:
    {{- include "embed-text.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "embed-text.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
