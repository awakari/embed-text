# Default values for embed-text.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ghcr.io/awakari/embed-text
  pullPolicy: Always
  tag: "latest"
imagePullSecrets:
  - name: github-registry
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

podAnnotations: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

service:
  type: ClusterIP
  port: 50051

ingress:
  enabled: false
  hosts:
    - host: embed-text.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: secret-embed-text-tls-server
      hosts:
        - embed-text.local

resources:
  requests:
    cpu: 1
    memory: 1Gi

autoscaling:
  enabled: true
  minReplicas: 1
  maxReplicas: 1
  targetCPUUtilizationValue: 100m
  targetMemoryUtilizationValue: 64Mi

priority:
  class: "awk-critical"

nodeSelector: {}

tolerations: []

log:
  # https://pkg.go.dev/golang.org/x/exp/slog#Level
  level: -4
model:
  path: "/model"
  name: "intfloat/multilingual-e5-small"
  chunk:
    size: 1024
    overlap: 192
  file:
    name: "model.onnx"
  intraOpThreads:
    enabled: true
    num: 1
